apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goget-server.fullname" . }}-test-connection"
  labels:
    {{- include "goget-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goget-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
