apiVersion: v1
kind: Service
metadata:
  name: {{ include "goget-server.fullname" . }}
  labels:
    {{- include "goget-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goget-server.selectorLabels" . | nindent 4 }}
